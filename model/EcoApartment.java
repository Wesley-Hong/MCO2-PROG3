package model;

/**
 * Extends property class and base price is still the same
 */
public class EcoApartment extends Property {

    /**
     * Constructor for new Eco Apartment Property
     * @param name property name
     */
    public EcoApartment(String name) {
        super(name);
    }
}
